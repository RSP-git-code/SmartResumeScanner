DJANGO_SECRET_KEY=change-me-to-a-random-secret
DEBUG=True

GOOGLE_API_KEY=AIza...
# Chat model used for extraction/scoring/interview-question chains.
GEMINI_MODEL=gemini-flash-lite-latest
# Embedding model used for RAG indexing.
GEMINI_EMBEDDING_MODEL=models/gemini-embedding-001

# Hybrid scoring weights (must sum to 1.0)
LLM_SCORE_WEIGHT=0.6
DETERMINISTIC_SCORE_WEIGHT=0.4
